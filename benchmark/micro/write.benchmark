# name: benchmark/micro/write.benchmark
# description: Write one uncompressed file
# group: [micro]

name Write
group micro

require nanoarrow

load
CREATE TABLE mixed AS SELECT i AS id, i % 1000 AS k, (i * 7919) % 100003 AS v, i / 7 AS d, 'item_' || (i % 50000) AS s FROM range(8000000) t(i);

run
COPY mixed TO '${BENCHMARK_DIR}/nanoarrow_write.arrows' (FORMAT ARROWS);

result I
8000000
