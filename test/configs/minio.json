{
  "description": "Run the tests that write their own files against MinIO, started by scripts/start-minio.sh.",
  "on_init": "CREATE SECRET (TYPE S3, KEY_ID 'admin', SECRET 'password', ENDPOINT '127.0.0.1:9000', URL_STYLE 'path', USE_SSL false);",
  "test_env": [{
      "env_name": "DATA_PATH",
      "env_value": "s3://mybucket/{BASE_TEST_NAME}"
  }],
  "statically_loaded_extensions": [
    "core_functions",
    "httpfs"
  ]
}
