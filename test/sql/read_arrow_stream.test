# name: test/sql/read_arrow_stream.test
# description: test nanoarrow extension
# group: [nanoarrow]

# Require statement will ensure this test is run with this extension loaded
require nanoarrow

statement ok
COPY (SELECT 42 as foofy) TO "__TEST_DIR__/test.arrows" (FORMAT ARROWS);

query I
SELECT count(*) FROM read_arrow_stream('__TEST_DIR__/test.arrows');
----
1

query I
SELECT foofy FROM read_arrow_stream('__TEST_DIR__/test.arrows')
----
42
